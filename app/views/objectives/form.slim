= simple_form_for [@tree, @objective] do |f|
  = f.input :parent_id, collection: @objective.potential_parents, label: "Parent", selected: params[:parent_id] || @objective.parent_id, include_blank: true
  = f.input :title
  = f.submit "Save"

